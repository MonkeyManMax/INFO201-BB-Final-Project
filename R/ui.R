library(shiny)

shinyUI(fluidPage(

    # Application title
    titlePanel(""),

    # Sidebar
    sidebarLayout(
        sidebarPanel(
            
        ),

        # Main Panel
        mainPanel(

        )
    )
))
